version: '3.8'

services:
  ppt-agent:
    build:
      context: ..
      dockerfile: ppt-agent/Dockerfile
    container_name: agentmesh-ppt-agent
    ports:
      - "8004:8004"
    environment:
      - PORT=8004
      - AGENT_NAME=Pitch Deck & Strategy Agent
    restart: unless-stopped
