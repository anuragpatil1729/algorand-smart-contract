version: '3.8'

services:
  testing-agent:
    build:
      context: ..
      dockerfile: testing-agent/Dockerfile
    container_name: agentmesh-testing-agent
    ports:
      - "8005:8005"
    environment:
      - PORT=8005
      - AGENT_NAME=Automated QA & Security Agent
    restart: unless-stopped
